@model List<Blog.DAL.DataModel.Etiket>

<h3 class="title">Etiketler</h3>
<div class="tags-cloud">
    @foreach (var e in Model)
    {
        <div class="tag">@Html.ActionLink(@e.Icerik, "MakalelerByEtiket", new { etiketID = e.EtiketID })</div>
    }
</div>
